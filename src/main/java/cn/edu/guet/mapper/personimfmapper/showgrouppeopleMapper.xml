<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.guet.mapper.personimfmapper.showgrouppeopleMapper">
    <select id="getgrouppeople" parameterType="String" resultType="grouppeople">
        SELECT users.username,dgroup.gcount from users,(select gid from users where userid=#{values}) c,dgroup
        WHERE users.gid=c.gid AND dgroup.gid=c.gid;
    </select>
</mapper>